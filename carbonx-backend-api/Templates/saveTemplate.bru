meta {
  name: saveTemplate
  type: http
  seq: 4
}

post {
  url: {{host}}/api/templates/:collection/:key?database=default
  body: none
  auth: inherit
}

params:query {
  database: default
}

params:path {
  key: 1
  collection: products
}

settings {
  encodeUrl: true
  timeout: 0
}
