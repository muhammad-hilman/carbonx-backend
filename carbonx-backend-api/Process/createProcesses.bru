meta {
  name: createProcesses
  type: http
  seq: 5
}

post {
  url: {{host}}/api/processes
  body: json
  auth: inherit
}

body:json {
  [
    {
    "name": "combining",
    "type": "mixing"
    }
  ]
}

settings {
  encodeUrl: true
  timeout: 0
}
