meta {
  name: Create a cursor
  type: http
  seq: 1
}

post {
  url: {{host}}/_db/{{databaseName}}/_api/cursor
  body: json
  auth: basic
}

auth:basic {
  username: root
  password: test
}

body:json {
  {
    "query" : "FOR v IN 1..100 INBOUND @startNode GRAPH default OPTIONS { bfs: true } FILTER v._id != @startNode RETURN v._id",
    "bindVars" :  {
                    "startNode" : "processes/2"
                  },
    "count" : true,
    "batchSize" : 10
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
